@await Html.PartialAsync("~/Views/ProjeElementleri/Sepet/_SepetModallari.cshtml")

    <section class="ms-siparis-asama-paneli ms-siparis-asama-paneli-aktif" data-ms-siparis-asama-panel="sepet">
    <div class="ms-sepet-mobil-sabit-aksiyon" aria-label="Mobil sepet aksiyonları">
        <div class="ms-sepet-mobil-sabit-tutar">
            <span>Ödenecek Tutar</span>
            <strong class="ms-urun-fiyat" data-ms-sepet-odenecek>0,00 TL</strong>
        </div>
        <button class="ms-buton ms-buton-l ms-buton-satin-al ms-buton-tam" type="button">Siparişi Tamamla</button>
    </div>
    <div class="ms-sepet-izgara">
        <section class="ms-sepet-liste-alani" aria-labelledby="ms-sepet-ornek-baslik">
            @* C7: eşik üzeri sepette ve TCKN doğrulanmamışsa script gösterir *@
            <div class="ms-sepet-tckn-uyari" role="alert" data-ms-tckn-banner hidden>
                <div>
                    <strong>TCKN doğrulama gerekli</strong>
                    <p data-ms-tckn-banner-metin>Eşik üzeri sipariş verdiğiniz için TCKN doğrulama yapmanız gerekiyor.</p>
                </div>
                <button class="ms-buton ms-buton-s ms-buton-tehlikeli" type="button" data-ms-tckn-modal-ac>Doğrula</button>
            </div>

            <div class="ms-sepet-ust">
                <div>
                    <h3 id="ms-sepet-ornek-baslik" class="ms-sepet-bolum-basligi">Sepetim</h3>
                    <p class="ms-sepet-bolum-aciklama" data-ms-sepet-ozet-aciklama>Sepetiniz yükleniyor…</p>
                </div>
                <label class="ms-sepet-secim">
                    <input name="sepet_tumunu_sec" type="checkbox" checked data-ms-sepet-tumunu-sec>
                    <span>Tümünü Seç</span>
                </label>
            </div>

            <div class="ms-sepet-satici-grubu">
                @if (false)
                {
                @* Faz H: kargo kuralları (bedava eşiği + tahmini teslim) gelince açılır *@
                <div class="ms-sepet-kart-bilgi-listesi">
                    <div class="ms-sepet-kart-bilgi ms-sepet-kart-bilgi-kargo">
                        <i class="fa-solid fa-truck ms-fa-ikon " aria-hidden="true"></i>
                    <span><strong class="ms-fiyat-standart">1.100 ₺</strong> daha ürün ekle, kargo bedava olsun.</span>
                    </div>
                    <div class="ms-sepet-kart-bilgi ms-sepet-kart-bilgi-teslimat">
                        <i class="fa-solid fa-clock ms-fa-ikon" aria-hidden="true"></i>
                        <span>Tahmini Kargoya Teslim: 3 gün içinde</span>
                    </div>
                </div>
                }
                @if (false)
                {
                @* E6: "sepeti koleksiyon olarak kaydet" koleksiyon backend'iyle; tek satıcı — başlık gizli *@
                <div class="ms-sepet-satici-baslik">
                    <strong>Misharix</strong>
                    <span class="ms-sepet-satici-rozetleri">
                        <button class="ms-buton ms-buton-s ms-buton-cizgili-birincil" type="button" data-ms-koleksiyon-modal-ac>
                            <i class="fa-solid fa-bookmark ms-fa-ikon" aria-hidden="true"></i>
                            Koleksiyon Ekle
                        </button>
                    </span>
                </div>
                }

                @* C1: satırlar canlı sepetten render edilir (aşağıdaki template + script);
                   kampanya etiketleri (G), teslimat satırı (H), eski fiyat (G) ve favoriye
                   taşı (E5/C2) veri kaynağı gelene dek şablona alınmadı. *@
                <div data-ms-sepet-satir-konteyner data-ms-tckn-esik="@(ViewData["MsTcknEsik"] ?? 13000)"></div>
                <p data-ms-sepet-bos hidden>Sepetiniz henüz boş. <a href="/">Alışverişe başlayın.</a></p>
                <template data-ms-sepet-satir-sablon>
                <article class="ms-sepet-satiri" data-ms-sepet-satir data-ms-birim-fiyat="">
                    <label class="ms-sepet-urun-secim">
                        <input type="checkbox" checked data-ms-sepet-urun-checkbox>
                        <span class="sr-only">Ürünü seç</span>
                    </label>
                    <a class="ms-sepet-gorsel-link" href="#" data-ms-sepet-gorsel-link>
                        <img class="ms-sepet-gorsel" src="" alt="" data-ms-sepet-gorsel>
                    </a>
                    <div class="ms-sepet-bilgi">
                        <a class="ms-sepet-basligi" href="#" data-ms-sepet-baslik></a>
                        <p class="ms-sepet-aciklama" data-ms-sepet-secenek></p>
                        <p class="ms-sepet-stok-uyari" data-ms-sepet-stok-uyari hidden>Stok tükendi. Bu ürünü sepetten çıkararak alışverişe devam edebilirsiniz.</p>
                        <div class="ms-sepet-alt-satir">
                            @* C9: tükendi satırında görünür (tasarımdaki tükendi örneğiyle aynı buton) *@
                            <button class="ms-sepet-tukendi-notu" type="button" data-ms-stok-haber-ver hidden>Stok yenilenince haber ver</button>
                            <div class="ms-adet-secici" data-ms-sepet-adet>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi azalt" data-ms-sepet-adet-azalt>-</button>
                                <span class="ms-adet-deger" data-ms-sepet-adet-deger>1</span>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi artır" data-ms-sepet-adet-artir>+</button>
                            </div>
                            <div class="ms-sepet-fiyat-alani">
                                <strong class="ms-urun-fiyat" data-ms-sepet-satir-tutar></strong>
                            </div>
                        </div>
                    </div>
                    <div class="ms-sepet-satir-aksiyonlar">
                        @* E5: favori butonu canlı şablona alındı (C1'de ertelenmişti) —
                           API + görsel toggle _FavoriDavranis'ta, kod satırın data'sından. *@
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Favorilere ekle" aria-pressed="false" data-ms-sepet-favori>
                            <img class="ms-ikon-siyah" src="/ikons/favorite.svg" alt="" aria-hidden="true">
                        </button>
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Ürünü sepetten kaldır" data-ms-sepet-sil>
                            <i class="fa-solid fa-trash-can ms-fa-ikon " aria-hidden="true"></i>
                        </button>
                    </div>
                </article>
                </template>

                @if (false)
                {
                <article class="ms-sepet-satiri" data-ms-sepet-satir data-ms-birim-fiyat="543.15">
                    <label class="ms-sepet-urun-secim">
                        <input name="sepet_urun_checkbox" type="checkbox" checked data-ms-sepet-urun-checkbox>
                        <span class="sr-only">Kadın yırtmaçlı kemerli tül eteği seç</span>
                    </label>
                    <a class="ms-sepet-gorsel-link" href="/urun-detay">
                        <img class="ms-sepet-gorsel" src="/images/ornek-resim.jpg" alt="Kadın yırtmaçlı kemerli kat detaylı tül etek" width="612" height="612" loading="lazy">
                    </a>
                    <div class="ms-sepet-bilgi">
                        <a class="ms-sepet-basligi" href="/urun-detay">Kadın Yırtmaçlı Kemerli Kat Detaylı Tül Etek</a>
                        <p class="ms-sepet-aciklama">Beden: M • Renk: Siyah</p>
                        <p class="ms-sepet-teslimat">Tahmini teslimat: 2 iş günü</p>
                        <div class="ms-sepet-kampanya-listesi" aria-label="Ürün kampanyaları">
                            <span class="ms-sepet-kampanya-etiketi">Sepette %10 indirim</span>
                            <span class="ms-sepet-kampanya-etiketi">Alt limit kargo bedava</span>
                            <span class="ms-sepet-kampanya-etiketi">Kartla ek %5</span>
                        </div>
                        <div class="ms-sepet-alt-satir">
                            <div class="ms-adet-secici" data-ms-sepet-adet>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi azalt" data-ms-sepet-adet-azalt>-</button>
                                <span class="ms-adet-deger" data-ms-sepet-adet-deger>1</span>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi artır" data-ms-sepet-adet-artir>+</button>
                            </div>
                            <div class="ms-sepet-fiyat-alani">
                                <span class="ms-urun-fiyat-eski">649,90 TL</span>
                                <span class="ms-sepet-kampanya-sonrasi">Kampanya sonrası</span>
                                <strong class="ms-urun-fiyat" data-ms-sepet-satir-tutar>543,15 TL</strong>
                            </div>
                        </div>
                    </div>
                    <div class="ms-sepet-satir-aksiyonlar">
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Favorilere ekle" aria-pressed="false" data-ms-sepet-favori>
                            <img class="ms-ikon-siyah" src="/ikons/favorite.svg" alt="" aria-hidden="true" width="20" height="20" loading="lazy">
                        </button>
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Ürünü sepetten kaldır" data-ms-sepet-sil>
                            <i class="fa-solid fa-trash-can ms-fa-ikon " aria-hidden="true"></i>
                        </button>
                    </div>
                </article>

                <article class="ms-sepet-satiri" data-ms-sepet-satir data-ms-birim-fiyat="399.90">
                    <label class="ms-sepet-urun-secim">
                        <input name="sepet_urun_checkbox" type="checkbox" checked data-ms-sepet-urun-checkbox>
                        <span class="sr-only">Kadın dik yaka gömleği seç</span>
                    </label>
                    <a class="ms-sepet-gorsel-link" href="/urun-detay">
                        <img class="ms-sepet-gorsel" src="/images/ornek-resim-2.jpg" alt="Kadın ön çıtçıtlı dik yaka gömlek" width="554" height="554" loading="lazy">
                    </a>
                    <div class="ms-sepet-bilgi">
                        <a class="ms-sepet-basligi" href="/urun-detay">Kadın Ön Çıtçıtlı Dik Yaka Gömlek</a>
                        <p class="ms-sepet-aciklama">Beden: S • Renk: Ekru</p>
                        <p class="ms-sepet-teslimat">Bugün kargoya verilir.</p>
                        <div class="ms-sepet-kampanya-listesi" aria-label="Ürün kampanyaları">
                            <span class="ms-sepet-kampanya-etiketi">2 al 1 öde fırsatı</span>
                            <span class="ms-sepet-kampanya-etiketi">Sepette ek %20</span>
                            <span class="ms-sepet-kampanya-etiketi">Hızlı teslimat</span>
                        </div>
                        <div class="ms-sepet-alt-satir">
                            <div class="ms-adet-secici" data-ms-sepet-adet>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi azalt" data-ms-sepet-adet-azalt>-</button>
                                <span class="ms-adet-deger" data-ms-sepet-adet-deger>2</span>
                                <button class="ms-adet-buton" type="button" aria-label="Adedi artır" data-ms-sepet-adet-artir>+</button>
                            </div>
                            <div class="ms-sepet-fiyat-alani">
                                <span class="ms-urun-fiyat-eski">499,90 TL</span>
                                <span class="ms-sepet-kampanya-sonrasi">Kampanya sonrası</span>
                                <strong class="ms-urun-fiyat" data-ms-sepet-satir-tutar>799,80 TL</strong>
                            </div>
                        </div>
                    </div>
                    <div class="ms-sepet-satir-aksiyonlar">
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Favorilere ekle" aria-pressed="false" data-ms-sepet-favori>
                            <img class="ms-ikon-siyah" src="/ikons/favorite.svg" alt="" aria-hidden="true" width="20" height="20" loading="lazy">
                        </button>
                        <button class="ms-sepet-ikon-buton" type="button" aria-label="Ürünü sepetten kaldır" data-ms-sepet-sil>
                            <i class="fa-solid fa-trash-can ms-fa-ikon " aria-hidden="true"></i>
                        </button>
                    </div>
                </article>

                <article class="ms-sepet-satiri ms-sepet-satiri-tukendi" aria-label="Stokta olmayan sepet ürünü">
                    <label class="ms-sepet-urun-secim">
                        <input name="checkbox_1" type="checkbox" disabled>
                        <span class="sr-only">Kadın desenli viskon elbise stokta yok</span>
                    </label>
                    <a class="ms-sepet-gorsel-link" href="/urun-detay">
                        <img class="ms-sepet-gorsel" src="/images/ornek-resim.jpg" alt="Kadın haki aerobin elbise" width="612" height="612" loading="lazy">
                        <span class="ms-sepet-gorsel-etiket">Tükendi</span>
                    </a>
                    <div class="ms-sepet-bilgi">
                        <a class="ms-sepet-basligi" href="/urun-detay">Kadın Haki Aerobin Kuşaklı Elbise</a>
                        <p class="ms-sepet-aciklama">Beden: 38 • Renk: Haki</p>
                        <p class="ms-sepet-stok-uyari">Stok tükendi. Bu ürünü sepetten çıkararak alışverişe devam edebilirsiniz.</p>
                        <div class="ms-sepet-kampanya-listesi" aria-label="Ürün kampanyaları">
                            <span class="ms-sepet-kampanya-etiketi">Sepette %15 indirim</span>
                            <span class="ms-sepet-kampanya-etiketi">2. ürüne ek %10</span>
                            <span class="ms-sepet-kampanya-etiketi">Kargo bedava</span>
                        </div>
                        <button class="ms-sepet-tukendi-sil-buton" type="button" data-ms-sepet-sil>
                            <i class="fa-solid fa-trash-can ms-fa-ikon " aria-hidden="true"></i>
                            Sepetten çıkar
                        </button>
                        <div class="ms-sepet-alt-satir">
                            <button class="ms-sepet-tukendi-notu" type="button" data-ms-stok-haber-ver>Stok yenilenince haber ver</button>
                            <div class="ms-sepet-fiyat-alani">
                                <span class="ms-urun-fiyat-eski">899,90 TL</span>
                                <span class="ms-sepet-kampanya-sonrasi">Kampanya sonrası</span>
                                <strong class="ms-urun-fiyat">764,90 TL</strong>
                            </div>
                        </div>
                    </div>
                    <div class="ms-sepet-satir-aksiyonlar" aria-hidden="true"></div>
                </article>
                }
            </div>

        </section>

        <aside class="ms-siparis-ozeti" aria-labelledby="ms-siparis-ozeti-baslik">
            <h3 id="ms-siparis-ozeti-baslik" class="ms-siparis-basligi">Sipariş Özeti</h3>
            @if (false)
            {
            @* Faz G/C3/H: etiket rozetleri (kargo/kupon/puan/kapıda) veri kaynaklarıyla açılır *@
            <div class="ms-siparis-etiket-listesi" aria-label="Sipariş etiketleri">
                <span class="ms-siparis-etiket-rozet ms-siparis-etiket-rozet-aktif ms-siparis-etiket-rozet-kargo">
                    <i class="fa-solid fa-truck ms-fa-ikon " aria-hidden="true"></i>
                    <span>Ücretsiz Kargo</span>
                </span>
                <span class="ms-siparis-etiket-rozet ms-siparis-etiket-rozet-kupon">
                    <i class="fa-solid fa-ticket ms-fa-ikon " aria-hidden="true"></i>
                    <span>Kupon Uygun</span>
                </span>
                <span class="ms-siparis-etiket-rozet ms-siparis-etiket-rozet-puan">
                    <i class="fa-solid fa-coins ms-fa-ikon " aria-hidden="true"></i>
                    <span>Puan Kazandırır</span>
                </span>
                <span class="ms-siparis-etiket-rozet ms-siparis-etiket-rozet-odeme">
                    <i class="fa-solid fa-money-bill-wave ms-fa-ikon " aria-hidden="true"></i>
                    <span>Kapıda Ödeme</span>
                </span>
            </div>
            }
            <div class="ms-siparis-satirlari">
                <div class="ms-siparis-satiri"><span class="ms-siparis-etiket">Ürün Toplamı</span><span class="ms-siparis-deger ms-fiyat-standart" data-ms-sepet-urun-toplami>0,00 TL</span></div>
                <div class="ms-siparis-satiri"><span class="ms-siparis-etiket">Kargo</span><span class="ms-siparis-deger ms-siparis-ucretsiz">Ücretsiz</span></div>
                @* C3: kupon uygulanınca görünür *@
                <div class="ms-siparis-satiri" data-ms-sepet-indirim-satiri hidden><span class="ms-siparis-etiket">Sepet İndirimi</span><span class="ms-siparis-deger ms-urun-fiyat-indirimli" data-ms-sepet-indirim>-0,00 TL</span></div>
            </div>
            <div class="ms-siparis-uygulanan-kupon" data-ms-uygulanan-kupon hidden>
                <div class="ms-siparis-uygulanan-kupon-bilgi">
                    <span>Uygulanan Kupon</span>
                    <strong data-ms-kupon-kod-yazi></strong>
                    <small data-ms-kupon-kosul></small>
                </div>
                <div class="ms-siparis-uygulanan-kupon-aksiyon">
                    <em class="ms-urun-fiyat-indirimli" data-ms-kupon-indirim-yazi></em>
                    <button class="ms-siparis-kupon-kaldir" type="button" data-ms-kupon-kaldir>Kuponu Kaldır</button>
                </div>
            </div>

            <label class="ms-siparis-kupon">
                <span>Kupon Kodu</span>
                <div class="ms-siparis-kupon-satiri">
                    <input name="kupon_kodu" type="text" placeholder="Kupon kodunuzu girin" data-ms-kupon-kodu>
                    <button class="ms-buton ms-buton-s ms-buton-satin-al" type="button" data-ms-kupon-uygula>Uygula</button>
                </div>
            </label>
            <div class="ms-siparis-kupon-bildirimleri" data-ms-kupon-bildirimleri>
                <div class="ms-siparis-kupon-bildirim ms-siparis-kupon-bildirim-basarili" data-ms-kupon-bildirim-basarili hidden>
                    <strong>Kupon uygulandı.</strong>
                    <span data-ms-kupon-bildirim-basarili-metin>Sepet indirimi sipariş özetine başarıyla yansıtıldı.</span>
                </div>
                <div class="ms-siparis-kupon-bildirim ms-siparis-kupon-bildirim-hata" data-ms-kupon-bildirim-hata hidden>
                    <strong>Geçersiz kupon.</strong>
                    <span data-ms-kupon-bildirim-hata-metin>Girdiğiniz kupon kodu kullanılamıyor veya kampanya koşullarını karşılamıyor.</span>
                </div>
            </div>

            <div class="ms-siparis-ozet-aksiyonlar">
                <a class="ms-buton ms-buton-m ms-buton-devam" href="/">Alışverişe Devam Et</a>
                @* E9: üyenin kupon listesi — modal _SepetModallari'nda GET account/coupons ile dolar. *@
                <button class="ms-buton ms-buton-m ms-buton-satin-al" type="button" data-ms-kupon-modal-ac>Kuponlarım</button>
            </div>

            <div class="ms-siparis-toplam">
                <span class="ms-siparis-toplam-etiket">Ödenecek Tutar</span>
                <span class="ms-siparis-toplam-deger ms-urun-fiyat" data-ms-sepet-odenecek>0,00 TL</span>
            </div>
            @* Sözleşme onay bloğu sepetten kaldırıldı (kullanıcı kararı 2026-07-30):
               onay zaten ödeme adımında zorunlu alınıyor — sepette çift onay kafa
               karıştırıyordu. "Siparişi Tamamla" onaysız /teslimat'a geçer (eski
               davranışta da sepetteki kutu denetlenmiyordu). *@
            <button class="ms-buton ms-buton-l ms-buton-satin-al ms-buton-tam" type="button">Siparişi Tamamla</button>
            <p class="ms-siparis-guvenli-odeme">Güvenli ödeme altyapısı ile bilgileriniz korunur.</p>
        </aside>
    </div>

    @* 2026-07-17: tasarımın ms-sepet-onceden bölümü — sepetten çıkarılan ürünler
       (localStorage "ms-sepet-onceden", silme onayında kaydedilir). Kayıt yokken bölüm
       gizli. Rozet/popülerlik satırları veri kaynağı gelene dek basılmaz (demo metin
       taşınmadı); Sepete Ekle kayıtlı varyant+fiyatla gerçek sepet API'sine gider. *@
    <section class="ms-sepet-onceden" aria-labelledby="ms-sepet-onceden-baslik" data-ms-sepet-onceden hidden>
        <div class="ms-sepet-onceden-baslik-alani">
            <h3 id="ms-sepet-onceden-baslik">Önceden Eklediklerim</h3>
        </div>
        <div class="ms-sepet-onceden-grid" data-ms-sepet-onceden-grid></div>
        <template data-ms-sepet-onceden-sablon>
            <article class="ms-sepet-onceden-kart">
                <a class="ms-sepet-onceden-gorsel" href="#" data-alan="gorsel-link">
                    <img src="" alt="" width="240" height="320" loading="lazy" data-alan="gorsel">
                </a>
                <div class="ms-sepet-onceden-bilgi">
                    <a class="ms-sepet-onceden-urun" href="#" data-alan="urun-link"></a>
                    <p class="ms-sepet-onceden-fiyat ms-urun-fiyat-satiri"><strong data-alan="fiyat"></strong></p>
                </div>
                <div class="ms-sepet-onceden-aksiyonlar">
                    <button class="ms-sepet-ikon-buton" type="button" aria-label="Favorilere ekle" aria-pressed="false" data-ms-sepet-favori>
                        <img class="ms-ikon-siyah" src="/ikons/favorite.svg" alt="" aria-hidden="true" width="20" height="20" loading="lazy">
                    </button>
                    <button class="ms-sepet-ikon-buton" type="button" aria-label="Listeden kaldır" data-ms-sepet-onceden-kaldir>
                        <i class="fa-solid fa-trash-can ms-fa-ikon " aria-hidden="true"></i>
                    </button>
                    <button class="ms-sepet-onceden-buton" type="button" data-ms-sepet-onceden-ekle>Sepete Ekle</button>
                </div>
            </article>
        </template>
    </section>
    </section>


    @* Bu script: TCKN modalı ve ilgili form doğrulama davranışını yönetir. *@
    <script>
    (() => {
        const sepetSayfasiKok = document.currentScript?.closest("[data-ms-sayfa-panel], .ms-panel") || document;
        // C7: demo TCKN mantığı kaldırıldı — gerçek modal davranışı + algoritma +
        // backend kaydı _SepetModallari sonundaki canlı script'te (iki sayfada da çalışır).

        // C3: demo kupon mantığı kaldırıldı — gerçek uygula/kaldır/yeniden-doğrulama
        // dosya sonundaki canlı sepet script'inde (promotion coupon/validate API'siyle).

        sepetSayfasiKok.querySelectorAll("[data-ms-sozlesme-modal-ac]").forEach((buton) => {
            buton.addEventListener("click", () => {
                const ozet = buton.closest(".ms-siparis-ozeti");
                window.msSozlesmeModalAc?.(ozet?.querySelector("[data-ms-sozlesme-onay-input]") || null);
            });
        });

        sepetSayfasiKok.querySelectorAll("[data-ms-sozlesme-onay-input]").forEach((checkbox) => {
            checkbox.addEventListener("click", (event) => {
                if (!checkbox.checked) {
                    return;
                }

                event.preventDefault();
                checkbox.checked = false;
                window.msSozlesmeModalAc?.(checkbox);
            });
        });

        sepetSayfasiKok.querySelectorAll("[data-ms-sepet-tumunu-sec]").forEach((tumunuSec) => {
            const sepetAlani = tumunuSec.closest(".ms-sepet-liste-alani") || sepetSayfasiKok;
            const urunSecimleri = Array.from(sepetAlani.querySelectorAll("[data-ms-sepet-urun-checkbox]"));

            if (!urunSecimleri.length) {
                return;
            }

            const tumunuSecDurumuGuncelle = () => {
                const seciliAdet = urunSecimleri.filter((secim) => secim.checked).length;
                tumunuSec.checked = seciliAdet === urunSecimleri.length;
                tumunuSec.indeterminate = seciliAdet > 0 && seciliAdet < urunSecimleri.length;
            };

            tumunuSec.addEventListener("change", () => {
                urunSecimleri.forEach((secim) => {
                    secim.checked = tumunuSec.checked;
                });
                tumunuSec.indeterminate = false;
            });

            urunSecimleri.forEach((secim) => {
                secim.addEventListener("change", tumunuSecDurumuGuncelle);
            });

            tumunuSecDurumuGuncelle();
        });

        const favoriIkonuOlustur = (src, siyah = true) => {
            const ikon = document.createElement("img");
            ikon.src = src;
            ikon.alt = "";
            ikon.setAttribute("aria-hidden", "true");
            if (siyah) {
                ikon.classList.add("ms-ikon-siyah");
            }
                return ikon;
        };

        sepetSayfasiKok.querySelectorAll("[data-ms-sepet-favori]").forEach((buton) => {
            if (buton.dataset.msSepetFavoriHazir === "true") {
                return;
            }

            const butonIkonunuGuncelle = (aktif) => {
                buton.innerHTML = "";
                buton.appendChild(favoriIkonuOlustur(aktif ? "/ikons/kalp.svg" : "/ikons/favorite.svg"));
            };

            buton.dataset.msSepetFavoriHazir = "true";
            butonIkonunuGuncelle(buton.getAttribute("aria-pressed") === "true");

            buton.addEventListener("click", () => {
                const aktif = buton.classList.toggle("ms-sepet-ikon-buton-aktif");
                buton.setAttribute("aria-pressed", aktif.toString());
                buton.setAttribute("aria-label", aktif ? "Favorilerden çıkar" : "Favorilere ekle");
                butonIkonunuGuncelle(aktif);

                const kalp = document.createElement("span");
                kalp.className = "ms-urun-favori-ekran-kalp";
                kalp.appendChild(favoriIkonuOlustur(aktif ? "/ikons/kalp.svg" : "/ikons/kirik-kalp.svg", false));
                document.body.appendChild(kalp);
                kalp.addEventListener("animationend", () => kalp.remove(), { once: true });
            });
        });

        sepetSayfasiKok.querySelectorAll("[data-ms-sepet-sil]").forEach((buton) => {
            if (buton.dataset.msSepetSilHazir === "true") {
                return;
            }

            buton.dataset.msSepetSilHazir = "true";
            buton.addEventListener("click", () => {
                const satir = buton.closest(".ms-sepet-satiri");

                if (!satir) {
                    return;
                }

                const urunAdi = satir.querySelector(".ms-sepet-basligi")?.textContent?.replace(/\s+/g, " ").trim();
                const aciklama = urunAdi
                    ? `${urunAdi} sepetinizden çıkarılacak. Emin misiniz?`
                    : "Bu ürün sepetinizden çıkarılacak. Emin misiniz?";

                if (typeof window.msSepetSilModalAc === "function") {
                    window.msSepetSilModalAc(satir, aciklama);
                    return;
                }

                satir.remove();
            });
        });

        const paraFormatla = (deger) => `${deger.toLocaleString("tr-TR", {
            minimumFractionDigits: 2,
            maximumFractionDigits: 2
        })} TL`;

        sepetSayfasiKok.querySelectorAll("[data-ms-sepet-satir]").forEach((satir) => {
            const adetAlani = satir.querySelector("[data-ms-sepet-adet]");
            const azalt = satir.querySelector("[data-ms-sepet-adet-azalt]");
            const artir = satir.querySelector("[data-ms-sepet-adet-artir]");
            const adetDeger = satir.querySelector("[data-ms-sepet-adet-deger]");
            const satirTutar = satir.querySelector("[data-ms-sepet-satir-tutar]");
            const birimFiyat = Number(satir.dataset.msBirimFiyat || 0);

            if (!adetAlani || !azalt || !artir || !adetDeger || !satirTutar || !birimFiyat) {
                return;
            }

            const adetGuncelle = (yeniAdet) => {
                const adet = Math.max(1, Math.min(yeniAdet, 10));
                adetDeger.textContent = adet.toString();
                satirTutar.textContent = paraFormatla(birimFiyat * adet);
                azalt.disabled = adet <= 1;
                artir.disabled = adet >= 10;
            };

            azalt.addEventListener("click", () => adetGuncelle(Number(adetDeger.textContent || 1) - 1));
            artir.addEventListener("click", () => adetGuncelle(Number(adetDeger.textContent || 1) + 1));
            adetGuncelle(Number(adetDeger.textContent || 1));
        });
    })();
</script>

@* C1: Canlı sepet script'i — satırlar GET /api/store/cart'tan (B5 mini sepet deseniyle aynı
   oturum anahtarları), adet PUT / silme DELETE (silme onayı misharix'in sil modalıyla:
   msSepetSilModalAc + onay butonuna eklenen ikinci dinleyici API'yi çağırır). Tümünü seç /
   satır checkbox'ları yalnız özet toplamını etkiler (sipariş C10'da tam sepetle kurulur).
   Yukarıdaki misharix demo script'i satır bulamadığından satır-düzeyi bağlamaları etkisizdir;
   TCKN/sözleşme modal davranışları ondan çalışmaya devam eder. *@
<script>
document.addEventListener("DOMContentLoaded", () => {
    const platformId = document.querySelector("[data-ms-arama]")?.dataset.msAramaPlatform || "";
    const konteyner = document.querySelector("[data-ms-sepet-satir-konteyner]");
    const sablon = document.querySelector("[data-ms-sepet-satir-sablon]");
    if (!platformId || !konteyner || !sablon) { return; }

    const bosMesaj = document.querySelector("[data-ms-sepet-bos]");
    const ozetAciklama = document.querySelector("[data-ms-sepet-ozet-aciklama]");
    const urunToplami = document.querySelector("[data-ms-sepet-urun-toplami]");
    const odenecekler = [...document.querySelectorAll("[data-ms-sepet-odenecek]")];
    const tumunuSec = document.querySelector("[data-ms-sepet-tumunu-sec]");
    const kuponKarti = document.querySelector("[data-ms-uygulanan-kupon]");
    const kuponInput = document.querySelector("[data-ms-kupon-kodu]");
    const indirimSatiri = document.querySelector("[data-ms-sepet-indirim-satiri]");
    const indirimAlani = document.querySelector("[data-ms-sepet-indirim]");
    const bildirimBasarili = document.querySelector("[data-ms-kupon-bildirim-basarili]");
    const bildirimHata = document.querySelector("[data-ms-kupon-bildirim-hata]");

    const paraYaz = (deger) => new Intl.NumberFormat("tr-TR",
        { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(deger || 0) + " TL";
    const trAd = (i18n) => (i18n && (i18n.tr || Object.values(i18n)[0])) || "";

    // Sepet seçimi (checkout'a girecek kalemler): seçili OLMAYAN variantId'ler sessionStorage'da
    // tutulur; teslimat/ödeme adımları bunu okuyup özeti + siparişi buna göre kurar (2026-07-31).
    const SECIM_ANAHTARI = "msSepetSecimDisi";
    const secimDisiOku = () => {
        try { return new Set(JSON.parse(sessionStorage.getItem(SECIM_ANAHTARI) || "[]")); }
        catch { return new Set(); }
    };
    const secimiKaydet = () => {
        const disi = [];
        konteyner.querySelectorAll("[data-ms-sepet-satir]").forEach((satir) => {
            const kutu = satir.querySelector("[data-ms-sepet-urun-checkbox]");
            const vid = satir.dataset.msVariant;
            if (vid && kutu && !kutu.checked) { disi.push(vid); }
        });
        try { sessionStorage.setItem(SECIM_ANAHTARI, JSON.stringify(disi)); } catch { /* dolu/kapalı */ }
    };

    let sepetId = null;
    let silinecekKalemId = null;
    const kalemBilgileri = new Map(); // kalemId → kalem (silme anında "önceden" kaydı için)

    // ── 2026-07-17: "Önceden Eklediklerim" (tasarımın ms-sepet-onceden bölümü) ──
    // Üye girişinde kaynak SUNUCU (api/store/cart/removed — üyeye bağlı kalıcı geçmiş,
    // cihazlar arası ortak); misafirde localStorage (varyant başına tek, en yeni 8).
    // Üye girişiyle sayfaya gelindiğinde misafirken biriken yerel kayıtlar sunucuya taşınır.
    // Sepete Ekle kayıtlı varyant+fiyatla gerçek sepet API'sine gider, başarıda listeden düşer.
    const ONCEDEN_ANAHTARI = "ms-sepet-onceden";
    const oncedenAlan = document.querySelector("[data-ms-sepet-onceden]");
    const oncedenGrid = document.querySelector("[data-ms-sepet-onceden-grid]");
    const oncedenSablon = document.querySelector("[data-ms-sepet-onceden-sablon]");
    const uyeTokeni = () => localStorage.getItem("ecspros_member_token");

    const oncedenOku = () => {
        try { return JSON.parse(localStorage.getItem(ONCEDEN_ANAHTARI)) || []; }
        catch { return []; }
    };
    const oncedenYaz = (kayitlar) => {
        try { localStorage.setItem(ONCEDEN_ANAHTARI, JSON.stringify(kayitlar.slice(0, 8))); }
        catch { /* dolu/kapalı storage */ }
    };

    const oncedenSunucuyaYaz = async (kayit) => {
        const cevap = await fetch("/api/store/cart/removed", {
            method: "POST",
            headers: { "Content-Type": "application/json", Authorization: "Bearer " + uyeTokeni() },
            body: JSON.stringify({
                firmPlatformId: platformId,
                variantId: kayit.varyant,
                productCode: kayit.kod || "",
                name: kayit.ad || "",
                imageUrl: kayit.gorsel || null,
                price: kayit.fiyat || 0,
                currencyCode: kayit.para || "TRY"
            })
        });
        const veri = await cevap.json().catch(() => null);
        if (!veri?.success) { throw new Error(veri?.error || "kayıt yazılamadı"); }
    };

    const oncedenSunucudanGetir = async () => {
        const cevap = await fetch(`/api/store/cart/removed?firmPlatformId=${platformId}`, {
            headers: { Authorization: "Bearer " + uyeTokeni() }
        });
        const veri = await cevap.json().catch(() => null);
        if (!veri?.success) { return []; }
        return (veri.data || []).map((k) => ({
            kod: k.productCode, varyant: k.variantId, ad: k.name,
            gorsel: k.imageUrl, fiyat: k.price, para: k.currencyCode
        }));
    };

    const oncedenKayitDusur = async (varyant) => {
        if (uyeTokeni()) {
            try {
                await fetch(`/api/store/cart/removed/${varyant}?firmPlatformId=${platformId}`, {
                    method: "DELETE",
                    headers: { Authorization: "Bearer " + uyeTokeni() }
                });
            } catch { /* sonraki çizimde gerçek durum gelir */ }
        } else {
            oncedenYaz(oncedenOku().filter((k) => k.varyant !== varyant));
        }
    };

    const oncedenCiz = async () => {
        if (!oncedenAlan || !oncedenGrid || !oncedenSablon) { return; }
        let kayitlar;
        if (uyeTokeni()) {
            try { kayitlar = await oncedenSunucudanGetir(); } catch { kayitlar = []; }
        } else {
            kayitlar = oncedenOku();
        }
        oncedenAlan.hidden = kayitlar.length === 0;
        oncedenGrid.innerHTML = "";
        kayitlar.forEach((kayit) => {
            const kart = oncedenSablon.content.firstElementChild.cloneNode(true);
            kart.dataset.msUrunKod = kayit.kod || ""; // favori butonu kodu buradan çözer
            const url = kayit.kod ? "/urun/" + encodeURIComponent(kayit.kod) : "#";
            const gorsel = kart.querySelector('[data-alan="gorsel"]');
            const urunLink = kart.querySelector('[data-alan="urun-link"]');
            kart.querySelector('[data-alan="gorsel-link"]').href = url;
            urunLink.href = url;
            urunLink.textContent = kayit.ad || kayit.kod || "Ürün";
            gorsel.src = kayit.gorsel || "/images/no-image.svg";
            gorsel.alt = kayit.ad || "";
            kart.querySelector('[data-alan="fiyat"]').textContent = paraYaz(kayit.fiyat);
            const favoriButonu = kart.querySelector("[data-ms-sepet-favori]");
            if (favoriButonu && kayit.kod && window.msFavoriKodlar?.has(window.msFavoriAnahtar ? window.msFavoriAnahtar(kayit.kod, null) : kayit.kod)) {
                window.msSepetFavoriIsaretle?.(favoriButonu, true);
            }
            kart.querySelector("[data-ms-sepet-onceden-kaldir]").addEventListener("click", async () => {
                await oncedenKayitDusur(kayit.varyant);
                oncedenCiz();
            });
            kart.querySelector("[data-ms-sepet-onceden-ekle]").addEventListener("click", async (event) => {
                const buton = event.currentTarget;
                buton.disabled = true;
                try {
                    // 2026-07-22: üyeyken token da gönderilir — kalem üye sepetine yazılır
                    const uyeToken = localStorage.getItem("ecspros_member_token");
                    const cevap = await fetch("/api/store/cart/items", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                            ...(uyeToken ? { Authorization: "Bearer " + uyeToken } : {})
                        },
                        body: JSON.stringify({
                            firmPlatformId: platformId,
                            variantId: kayit.varyant,
                            quantity: 1,
                            price: kayit.fiyat || 0,
                            currencyCode: kayit.para || "TRY",
                            sessionId: localStorage.getItem("ecspros_sid")
                        })
                    });
                    const veri = await cevap.json().catch(() => null);
                    if (veri?.success) {
                        if (veri.data?.cartId) { localStorage.setItem("ecspros_cart", veri.data.cartId); }
                        await oncedenKayitDusur(kayit.varyant);
                        oncedenCiz();
                        window.msMiniSepetYenile?.();
                        yenile();
                        return;
                    }
                    buton.disabled = false;
                } catch { buton.disabled = false; }
            });
            oncedenGrid.appendChild(kart);
        });
    };

    const oncedenKaydet = async (kalem) => {
        if (!kalem?.variantId) { return; }
        const kayit = {
            kod: kalem.productCode || "",
            varyant: kalem.variantId,
            ad: trAd(kalem.productNameI18n) || kalem.productCode || "Ürün",
            gorsel: kalem.imageUrl || "",
            fiyat: kalem.addedPrice || 0,
            para: kalem.currencyCode || "TRY"
        };
        if (uyeTokeni()) {
            try { await oncedenSunucuyaYaz(kayit); }
            catch { /* geçmiş kaydı düşmezse sepet silmesi yine de sürer */ }
        } else {
            oncedenYaz([kayit, ...oncedenOku().filter((k) => k.varyant !== kayit.varyant)]);
        }
        oncedenCiz();
    };

    // Misafirken biriken yerel kayıtlar üye girişinde bir kez sunucuya taşınır
    // (herhangi bir kayıt taşınamazsa yerel liste korunur, sonraki ziyarette denenir).
    const oncedenYereldenTasi = async () => {
        if (!uyeTokeni()) { return; }
        const yereller = oncedenOku();
        if (yereller.length === 0) { return; }
        try {
            for (const kayit of yereller) {
                if (kayit?.varyant) { await oncedenSunucuyaYaz(kayit); }
            }
            localStorage.removeItem(ONCEDEN_ANAHTARI);
        } catch { /* taşınamayanlar yerelde kalır */ }
    };

    oncedenYereldenTasi().then(oncedenCiz);

    const toplamlariGuncelle = () => {
        let toplam = 0;
        konteyner.querySelectorAll("[data-ms-sepet-satir]").forEach((satir) => {
            const secili = satir.querySelector("[data-ms-sepet-urun-checkbox]")?.checked;
            const adet = Number(satir.querySelector("[data-ms-sepet-adet-deger]")?.textContent || 0);
            const birim = Number(satir.dataset.msBirimFiyat || 0);
            if (secili) { toplam += birim * adet; }
        });
        if (urunToplami) { urunToplami.textContent = paraYaz(toplam); }
        const indirim = kuponDurum?.tutar || 0;
        const odenecek = Math.max(0, toplam - indirim);
        if (indirimAlani) { indirimAlani.textContent = "-" + paraYaz(indirim); }
        if (indirimSatiri) { indirimSatiri.hidden = !kuponDurum; }
        odenecekler.forEach((alan) => { alan.textContent = paraYaz(odenecek); });
        kuponuTazele(toplam);
        tcknBanner(odenecek);
        secimiKaydet();   // her seçim/adet değişiminde seçim durumunu kalıcılaştır
    };

    // C7: eşik üzeri + TCKN doğrulanmamış → uyarı banner'ı (asıl güvence checkout guard'ında)
    const tcknEsik = Number(document.querySelector("[data-ms-sepet-satir-konteyner]")?.dataset.msTcknEsik || 13000);
    const tcknBanner = (odenecek) => {
        const banner = document.querySelector("[data-ms-tckn-banner]");
        if (!banner) { return; }
        const gerekli = odenecek >= tcknEsik && window.msTcknDogrulandi !== true;
        banner.hidden = !gerekli;
        if (gerekli) {
            const metin = banner.querySelector("[data-ms-tckn-banner-metin]");
            if (metin) { metin.textContent = `${tcknEsik.toLocaleString("tr-TR")} TL üzeri sipariş verdiğiniz için TCKN doğrulama yapmanız gerekiyor.`; }
        }
    };
    window.addEventListener("ms:tckn-dogrulandi", () => tcknBanner(0));

    const seciliToplam = () => {
        let toplam = 0;
        konteyner.querySelectorAll("[data-ms-sepet-satir]").forEach((satir) => {
            if (satir.querySelector("[data-ms-sepet-urun-checkbox]")?.checked) {
                toplam += Number(satir.dataset.msBirimFiyat || 0)
                    * Number(satir.querySelector("[data-ms-sepet-adet-deger]")?.textContent || 0);
            }
        });
        return toplam;
    };

    const tumunuSecDurumu = () => {
        const kutular = [...konteyner.querySelectorAll("[data-ms-sepet-urun-checkbox]:not(:disabled)")];
        if (!tumunuSec || kutular.length === 0) { return; }
        const secili = kutular.filter((k) => k.checked).length;
        tumunuSec.checked = secili === kutular.length;
        tumunuSec.indeterminate = secili > 0 && secili < kutular.length;
    };

    const adetGonder = async (kalemId, adet) => {
        try {
            await fetch(`/api/store/cart/${sepetId}/items/${kalemId}`, {
                method: "PUT",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ quantity: adet })
            });
            window.msMiniSepetYenile?.();
        } catch { yenile(); }
    };

    const satirKur = (kalem) => {
        const satir = sablon.content.firstElementChild.cloneNode(true);
        satir.dataset.msBirimFiyat = String(kalem.addedPrice || 0);
        satir.dataset.msKalemId = kalem.id;
        satir.dataset.msVariant = kalem.variantId || "";
        // Önceki seçim durumunu geri yükle (sayfa yenilense de seçim korunur)
        if (kalem.variantId && secimDisiOku().has(kalem.variantId)) {
            const sk = satir.querySelector("[data-ms-sepet-urun-checkbox]");
            if (sk) { sk.checked = false; }
        }
        // E5: favori butonu satırın ürün kodunu buradan okur (_FavoriDavranis capture
        // dinleyicisi API'yi + görsel toggle'ı yönetir); mevcut favori işaretlenir.
        satir.dataset.msUrunKod = kalem.productCode || "";
        const favoriButonu = satir.querySelector("[data-ms-sepet-favori]");
        if (favoriButonu && kalem.productCode && window.msFavoriKodlar?.has(window.msFavoriAnahtar ? window.msFavoriAnahtar(kalem.productCode, null) : kalem.productCode)) {
            window.msSepetFavoriIsaretle?.(favoriButonu, true);
        }

        const ad = trAd(kalem.productNameI18n) || kalem.productCode || "Ürün";
        const detayUrl = kalem.productCode ? "/urun/" + encodeURIComponent(kalem.productCode) : "#";
        const gorselLink = satir.querySelector("[data-ms-sepet-gorsel-link]");
        const gorsel = satir.querySelector("[data-ms-sepet-gorsel]");
        const baslik = satir.querySelector("[data-ms-sepet-baslik]");
        gorselLink.href = detayUrl;
        baslik.href = detayUrl;
        baslik.textContent = ad;
        if (kalem.imageUrl) { gorsel.src = kalem.imageUrl; }
        gorsel.alt = ad;
        satir.querySelector("[data-ms-sepet-secenek]").textContent =
            (kalem.optionsText || "").replaceAll(", ", " • ");
        satir.querySelector(".sr-only").textContent = ad + " seç";

        const adetDeger = satir.querySelector("[data-ms-sepet-adet-deger]");
        const azalt = satir.querySelector("[data-ms-sepet-adet-azalt]");
        const artir = satir.querySelector("[data-ms-sepet-adet-artir]");
        const satirTutar = satir.querySelector("[data-ms-sepet-satir-tutar]");
        const birim = kalem.addedPrice || 0;

        const adetYaz = (adet) => {
            adetDeger.textContent = String(adet);
            satirTutar.textContent = paraYaz(birim * adet);
            azalt.disabled = adet <= 1;
            artir.disabled = adet >= 10;
        };
        adetYaz(kalem.quantity || 1);

        const adetDegistir = (fark) => {
            const yeni = Math.max(1, Math.min(10, Number(adetDeger.textContent || 1) + fark));
            if (yeni === Number(adetDeger.textContent)) { return; }
            adetYaz(yeni);
            toplamlariGuncelle();
            adetGonder(kalem.id, yeni);
        };
        azalt.addEventListener("click", () => adetDegistir(-1));
        artir.addEventListener("click", () => adetDegistir(1));

        // B12/C1: tükenen kalem tasarımdaki tükendi görünümüyle (checkbox pasif, adet gizli)
        if (kalem.isAvailable === false) {
            satir.classList.add("ms-sepet-satiri-tukendi");
            const kutu = satir.querySelector("[data-ms-sepet-urun-checkbox]");
            kutu.checked = false;
            kutu.disabled = true;
            satir.querySelector("[data-ms-sepet-stok-uyari]").hidden = false;
            satir.querySelector("[data-ms-sepet-adet]").hidden = true;

            // C9: stok gelince haber ver — üye POST atar, misafir giriş modalına yönlenir
            const haberButon = satir.querySelector("[data-ms-stok-haber-ver]");
            haberButon.hidden = false;
            haberButon.dataset.msStokHaberVaryant = kalem.variantId || "";
            haberButon.addEventListener("click", async () => {
                if (!localStorage.getItem("ecspros_member_token")) {
                    document.querySelector("[data-ms-giris-modal-ac]")?.click();
                    return;
                }
                haberButon.disabled = true;
                try {
                    const cevap = await fetch("/api/store/stock-alerts", {
                        method: "POST",
                        headers: { "Content-Type": "application/json", ...uyeBasligi() },
                        body: JSON.stringify({
                            firmPlatformId: platformId,
                            variantId: kalem.variantId,
                            productCode: kalem.productCode || null,
                            variantInfo: kalem.optionsText || null
                        })
                    });
                    const veri = await cevap.json().catch(() => null);
                    if (veri?.success) { stokHaberIsaretle(haberButon); }
                    else { haberButon.disabled = false; }
                } catch { haberButon.disabled = false; }
            });
        }

        satir.querySelector("[data-ms-sepet-urun-checkbox]").addEventListener("change", () => {
            toplamlariGuncelle();
            tumunuSecDurumu();
        });

        satir.querySelector("[data-ms-sepet-sil]").addEventListener("click", () => {
            silinecekKalemId = kalem.id;
            const aciklama = `${ad} sepetinizden çıkarılacak. Emin misiniz?`;
            if (typeof window.msSepetSilModalAc === "function") {
                window.msSepetSilModalAc(satir, aciklama);
            } else {
                satir.remove();
                silmeOnaylandi();
            }
        });

        return satir;
    };

    const silmeOnaylandi = async () => {
        if (!silinecekKalemId || !sepetId) { return; }
        const kalemId = silinecekKalemId;
        silinecekKalemId = null;
        oncedenKaydet(kalemBilgileri.get(kalemId)); // "Önceden Eklediklerim" bölümüne düşer
        try {
            await fetch(`/api/store/cart/${sepetId}/items/${kalemId}`, { method: "DELETE" });
        } catch { /* yenile gerçek durumu getirir */ }
        window.msMiniSepetYenile?.();
        yenile();
    };
    // Misharix'in sil onay modalındaki onay butonu satırı DOM'dan kaldırır (kendi dinleyicisi);
    // bu ikinci dinleyici aynı tıklamada API silmesini yapar.
    document.querySelector("[data-ms-sepet-sil-onayla]")?.addEventListener("click", silmeOnaylandi);
    document.querySelectorAll("[data-ms-sepet-sil-modal-kapat]").forEach((kapat) => {
        kapat.addEventListener("click", () => { silinecekKalemId = null; });
    });

    // ── C3: Kupon — gerçek doğrulama (promotion coupon/validate; use kaydı C10 checkout'ta).
    // Tasarımın sayfalar-arası kupon durumu sözleşmesi korunur: window.msSepetKuponDurumu +
    // sessionStorage msSepetKuponDurumu + "ms:sepet-kupon-degisti" event'i (ödeme sayfası okur).
    let kuponDurum = null;
    let kuponTazeleZamanlayici = null;

    const kuponDurumunuYayinla = () => {
        window.msSepetKuponDurumu = kuponDurum;
        try {
            if (kuponDurum) { sessionStorage.setItem("msSepetKuponDurumu", JSON.stringify(kuponDurum)); }
            else { sessionStorage.removeItem("msSepetKuponDurumu"); }
        } catch { /* depolama kapalı */ }
        window.dispatchEvent(new CustomEvent("ms:sepet-kupon-degisti", { detail: kuponDurum }));
    };

    const kuponBildir = (basarili, mesaj) => {
        if (bildirimBasarili) {
            bildirimBasarili.hidden = !basarili;
            if (basarili && mesaj) { bildirimBasarili.querySelector("[data-ms-kupon-bildirim-basarili-metin]").textContent = mesaj; }
        }
        if (bildirimHata) {
            bildirimHata.hidden = basarili;
            if (!basarili && mesaj) { bildirimHata.querySelector("[data-ms-kupon-bildirim-hata-metin]").textContent = mesaj; }
        }
    };

    const kuponKartiniCiz = () => {
        if (!kuponKarti) { return; }
        kuponKarti.hidden = !kuponDurum;
        if (!kuponDurum) { return; }
        kuponKarti.querySelector("[data-ms-kupon-kod-yazi]").textContent = kuponDurum.kod;
        kuponKarti.querySelector("[data-ms-kupon-kosul]").textContent = kuponDurum.aciklama || "Tüm Ödeme Yöntemleri";
        kuponKarti.querySelector("[data-ms-kupon-indirim-yazi]").textContent = "-" + paraYaz(kuponDurum.tutar);
    };

    const kuponKaldir = (mesaj) => {
        kuponDurum = null;
        kuponKartiniCiz();
        kuponDurumunuYayinla();
        if (mesaj) { kuponBildir(false, mesaj); }
        toplamlariGuncelle();
    };

    const uyeBasligi = () => {
        const token = localStorage.getItem("ecspros_member_token");
        return token ? { Authorization: "Bearer " + token } : {};
    };

    // C9: stok haber butonu durumu — kayıt varsa metin/pasiflik; ilk yüklemede API'den işaretlenir
    const stokHaberIsaretle = (buton) => {
        buton.textContent = "Stok gelince haber verilecek ✓";
        buton.disabled = true;
    };
    const stokHaberDurumunuIsaretle = async () => {
        if (!localStorage.getItem("ecspros_member_token")) { return; }
        const butonlar = [...konteyner.querySelectorAll("[data-ms-stok-haber-ver]:not([hidden])")];
        if (!butonlar.length) { return; }
        const idler = butonlar.map((b) => b.dataset.msStokHaberVaryant).filter(Boolean);
        try {
            const cevap = await fetch(`/api/store/stock-alerts?firmPlatformId=${platformId}&variantIds=${idler.join(",")}`,
                { headers: uyeBasligi() });
            const veri = await cevap.json().catch(() => null);
            (veri?.data || []).forEach((vid) => {
                butonlar.filter((b) => b.dataset.msStokHaberVaryant === vid).forEach(stokHaberIsaretle);
            });
        } catch { /* işaretlenemezse buton tıklanabilir kalır — POST idempotent */ }
    };

    const kuponDogrula = async (kod, toplam) => {
        const cevap = await fetch("/api/store/checkout/coupon/validate", {
            method: "POST",
            headers: { "Content-Type": "application/json", ...uyeBasligi() },
            body: JSON.stringify({ code: kod, cartTotal: toplam })
        });
        return cevap.json().catch(() => null);
    };

    const kuponUygula = async (kod, sessiz) => {
        const temizKod = (kod || "").trim().toLocaleUpperCase("tr-TR");
        if (!temizKod) { return; }
        try {
            const veri = await kuponDogrula(temizKod, seciliToplam());
            if (!veri?.success) {
                if (!sessiz) { kuponBildir(false, veri?.error || "Kupon kodu kullanılamıyor."); }
                else { kuponKaldir(); }
                return;
            }
            kuponDurum = {
                couponId: veri.data.couponId,
                kod: temizKod,
                odemeTipi: "tum",
                tutar: veri.data.discountAmount || 0,
                indirim: "-" + paraYaz(veri.data.discountAmount || 0),
                aciklama: veri.data.discountDescription || ""
            };
            kuponKartiniCiz();
            kuponDurumunuYayinla();
            if (!sessiz) { kuponBildir(true, kuponDurum.aciklama || "Sepet indirimi sipariş özetine yansıtıldı."); }
            if (kuponInput) { kuponInput.value = ""; }
            toplamlariGuncelle();
        } catch {
            if (!sessiz) { kuponBildir(false, "Kupon doğrulanamadı. Lütfen tekrar deneyin."); }
        }
    };

    // Sepet değişince uygulanan kuponu yeni toplamla sessizce yeniden doğrula
    function kuponuTazele(toplam) {
        if (!kuponDurum) { return; }
        window.clearTimeout(kuponTazeleZamanlayici);
        kuponTazeleZamanlayici = window.setTimeout(async () => {
            try {
                const veri = await kuponDogrula(kuponDurum.kod, toplam);
                if (!veri?.success) {
                    kuponKaldir(veri?.error || "Sepet değişti — kupon koşulları artık sağlanmıyor.");
                    return;
                }
                if ((veri.data.discountAmount || 0) !== kuponDurum.tutar) {
                    kuponDurum.tutar = veri.data.discountAmount || 0;
                    kuponDurum.indirim = "-" + paraYaz(kuponDurum.tutar);
                    kuponKartiniCiz();
                    kuponDurumunuYayinla();
                    toplamlariGuncelle();
                }
            } catch { /* geçici ağ hatası — mevcut kupon korunur */ }
        }, 400);
    }

    document.querySelector("[data-ms-kupon-uygula]")?.addEventListener("click", () => kuponUygula(kuponInput?.value, false));
    kuponInput?.addEventListener("keydown", (olay) => {
        if (olay.key === "Enter") { olay.preventDefault(); kuponUygula(kuponInput.value, false); }
    });
    document.querySelector("[data-ms-kupon-kaldir]")?.addEventListener("click", () => {
        kuponKaldir();
        kuponBildir(true, "Kupon kaldırıldı.");
        kuponInput?.focus();
    });

    tumunuSec?.addEventListener("change", () => {
        konteyner.querySelectorAll("[data-ms-sepet-urun-checkbox]:not(:disabled)").forEach((kutu) => {
            kutu.checked = tumunuSec.checked;
        });
        tumunuSec.indeterminate = false;
        toplamlariGuncelle();
    });

    const ciz = (sepet) => {
        const kalemler = sepet?.items || [];
        sepetId = sepet?.id || null;
        konteyner.innerHTML = "";
        kalemler.forEach((kalem) => {
            kalemBilgileri.set(kalem.id, kalem); // silme anında "önceden" kaydı buradan
            konteyner.appendChild(satirKur(kalem));
        });
        if (bosMesaj) { bosMesaj.hidden = kalemler.length > 0; }
        if (ozetAciklama) {
            ozetAciklama.textContent = kalemler.length > 0
                ? `${kalemler.reduce((t, k) => t + (k.quantity || 0), 0)} ürün sepete eklendi.`
                : "Sepetinizde ürün yok.";
        }
        toplamlariGuncelle();
        tumunuSecDurumu();
    };

    const yenile = async () => {
        const sid = localStorage.getItem("ecspros_sid");
        const cartId = localStorage.getItem("ecspros_cart");
        // 2026-07-22: üyeyken token da gönderilir — cartId/sid yokken de üye sepeti bulunur
        const uyeToken = localStorage.getItem("ecspros_member_token");
        if (!sid && !cartId && !uyeToken) { ciz(null); return; }
        try {
            const parametreler = new URLSearchParams({ firmPlatformId: platformId });
            if (cartId) { parametreler.set("cartId", cartId); }
            if (sid) { parametreler.set("sessionId", sid); }
            const cevap = await fetch("/api/store/cart?" + parametreler.toString(),
                uyeToken ? { headers: { Authorization: "Bearer " + uyeToken } } : undefined);
            const veri = await cevap.json().catch(() => null);
            ciz(veri?.success ? veri.data : null);
            stokHaberDurumunuIsaretle(); // C9: tükendi satırlarındaki mevcut kayıtları işaretle
        } catch { ciz(null); }
    };

    yenile().then(() => {
        try {
            // Hızlı satın alma askısı (2026-07-30): hızlı akış ödeme sayfası ana sepetin
            // kupon durumunu askıya alır — sepete dönüldüğünde geri yüklenir.
            const askida = sessionStorage.getItem("msSepetKuponDurumuAskida");
            if (askida !== null) {
                if (sessionStorage.getItem("msSepetKuponDurumu") === null) {
                    sessionStorage.setItem("msSepetKuponDurumu", askida);
                }
                sessionStorage.removeItem("msSepetKuponDurumuAskida");
            }
            const kayitli = JSON.parse(sessionStorage.getItem("msSepetKuponDurumu") || "null");
            if (kayitli?.kod) { kuponUygula(kayitli.kod, true); }
        } catch { /* bozuk kayıt yok sayılır */ }
    });

    // 2026-07-22: sepet sayfasındayken giriş yapılırsa liste reload'suz tazelenir —
    // ms:uye-giris, merge tamamlandıktan sonra tetiklenir (giriş modalı köprüsü).
    document.addEventListener("ms:uye-giris", () => { yenile(); });
});
</script>
